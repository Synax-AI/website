<footer class="obs-footer-premium">
  <div class="container">
    <div class="obs-footer-premium__grid">

      <!-- BRAND -->
      <div class="obs-footer-premium__brand">
     <a href="/" class="obs-footer-premium__logo">
  <img src="/images/logo/icons/synax-logo.png" alt="Synax" />
  <span class="obs-footer-brand__name">Synax</span>
</a>

      <p class="obs-footer-premium__tagline">
Extract and verify signals from filings — backed by evidence and full traceability.</p>

  <p class="obs-footer-premium__micro">
  Obsight — Forensic Workbench for Equity Analysts
</p>

<p class="obs-footer-premium__company">
 A product by Synax AI & Research Pvt. Ltd.
</p>

<div class="obs-footer-premium__trust">
  <i class="fas fa-shield-alt"></i>
 Built for serious equity analysts
</div>
      </div>

      <!-- PLATFORM -->
      <div class="obs-footer-premium__nav">
        <h6>Platform</h6>
        <ul>
          <li><a href="#problem">Problem</a></li>
          <li><a href="#features">Product</a></li>
          <li><a href="#why-obsight">Why Obsight</a></li>
        
          <li>
            <a href="#waitlist" class="obs-link-accent">
              Apply for Founding Access →
            </a>
          </li>
        </ul>
      </div>

      <!-- LEGAL -->
      <div class="obs-footer-premium__nav">
        <h6>Legal</h6>
        <ul>
          <li><a href="/terms">Terms & Conditions</a></li>
          <li><a href="/privacy">Privacy Policy</a></li>
        </ul>
      </div>

      <!-- CONTACT -->
      <div class="obs-footer-premium__contact">
        <h6>Contact</h6>

        <a href="mailto:info@synax.ai" class="obs-contact-email">
          info@synax.ai
        </a>

      <p class="obs-contact-note">
  Access · Partnerships · Product inquiries
</p>

<div class="obs-contact-phones">
  <a href="tel:+919893844539">+91 98938 44539</a>
  <a href="tel:+919893370027">+91 98933 70027</a>
</div>

<p class="obs-contact-address">
  Jabalpur, Madhya Pradesh, India
</p>

        

        <div class="obs-contact-socials">
          <a href="https://wa.me/9893844539" target="_blank" aria-label="WhatsApp">
            <i class="fa-brands fa-whatsapp"></i>
          </a>
          <a href="https://www.linkedin.com/company/synax-ai" target="_blank" aria-label="LinkedIn">
            <i class="fa-brands fa-linkedin-in"></i>
          </a>
        </div>
      </div>

    </div>

    <!-- BOTTOM -->
    <div class="obs-footer-premium__bottom">
      <div class="obs-footer-premium__copyright">
        © 2026 Synax AI & Research Pvt. Ltd. All rights reserved.
      </div>
    </div>
  </div>
</footer>

<style>

  /* --- BASE --- */
  .obs-contact-phones a {
  letter-spacing: 0.2px;
}
  .obs-link-accent {
  font-weight: 700 !important;
}
  .obs-footer-premium__micro {
    font-size: 0.85rem;
    color: var(--text-muted);
    margin-top: 16px;
    margin-bottom: 4px;
    font-weight: 500;
  }
  .obs-footer-premium__company {
    font-size: 0.8rem;
    color: var(--text-muted);
    margin-bottom: 24px;
  }
  .obs-contact-note {
    font-size: 0.85rem;
    color: var(--text-muted);
    margin-bottom: 24px;
    margin-top: 0;
  }
  .obs-footer-premium {
    background-color: var(--bg-primary);
    padding: 100px 0 40px;
    border-top: 1px solid var(--border-color);
  }

.obs-footer-premium__logo {
  display: flex;
  align-items: center;
  gap: 8px;
  text-decoration: none;
}

.obs-footer-premium__logo img {
  height: 32px;
  width: 32px;
}
.obs-footer-brand__name {
  font-size: 1.05rem;
  font-weight: 700;
  color: #1440a5;
  letter-spacing: 0.02em;
}

  /* --- GRID --- */
  .obs-footer-premium__grid {
    display: grid;
    grid-template-columns: 2.5fr 1fr 1fr 1.5fr; /* Perfectly balanced ratio */
    gap: 56px;
    padding-bottom: 72px;
  }

  /* --- BRAND --- */
 
  .obs-footer-premium__tagline {
    font-size: 0.95rem;
    line-height: 1.65;
    color: var(--text-secondary);
    max-width: 320px;
    margin-bottom: 20px;
    margin-top: 10px;
  }

  .obs-footer-premium__trust {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .obs-footer-premium__trust i {
    color: var(--brand-accent);
  }

  /* --- HEADINGS & NAV --- */
  .obs-footer-premium__nav h6,
  .obs-footer-premium__contact h6 {
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 24px 0;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .obs-footer-premium__nav ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .obs-footer-premium__nav li {
    margin-bottom: 12px;
  }

  .obs-footer-premium__nav a {
    font-size: 0.95rem;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s ease;
    font-weight: 500;
  }

  .obs-footer-premium__nav a:hover {
    color: var(--brand-accent);
  }

  .obs-link-accent {
    color: var(--brand-accent) !important;
    font-weight: 600 !important;
  }

  /* --- CONTACT COLUMN (UX Refined) --- */
  .obs-footer-premium__contact {
    display: flex;
    flex-direction: column;
  }

  .obs-contact-email {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--text-primary);
    text-decoration: none;
    margin-bottom: 16px;
    transition: color 0.2s ease;
  }

  .obs-contact-email:hover {
    color: var(--brand-accent);
  }

  .obs-contact-phones {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-bottom: 24px;
  }

  .obs-contact-phones a {
    font-size: 0.9rem;
    color: var(--text-secondary);
    text-decoration: none;
    font-family: "SF Mono", "Fira Code", monospace;
    transition: color 0.2s ease;
  }

  .obs-contact-phones a:hover {
    color: var(--brand-accent);
  }

  .obs-contact-address {
    font-size: 0.85rem;
    line-height: 1.6;
    color: var(--text-muted);
    margin: 0 0 24px 0;
  }

  /* --- SOCIALS --- */
  .obs-contact-socials {
    display: flex;
    gap: 12px;
  }

  .obs-contact-socials a {
    width: 38px;
    height: 38px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-secondary);
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-sm);
    font-size: 1.05rem;
    transition: all 0.2s ease;
  }

  .obs-contact-socials a:hover {
    color: var(--brand-accent);
    border-color: rgba(37, 99, 235, 0.3);
    background: var(--bg-primary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-sm);
  }

  /* --- BOTTOM BAR --- */
  .obs-footer-premium__bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-top: 1px solid var(--border-color);
    padding-top: 32px;
  }

  .obs-footer-premium__copyright {
    font-size: 0.85rem;
    color: var(--text-muted);
    font-weight: 500;
  }

  .obs-footer-premium__badge img {
    height: 36px;
    opacity: 0.6;
    transition: opacity 0.2s ease;
  }

  .obs-footer-premium__badge img:hover {
    opacity: 1;
  }

  /* --- RESPONSIVE --- */
  @media (max-width: 1024px) {
    .obs-footer-premium__grid {
      grid-template-columns: 1.5fr 1fr 1fr;
      row-gap: 56px;
    }
    .obs-footer-premium__contact {
      grid-column: 2 / -1;
    }
  }

  @media (max-width: 768px) {
    .obs-footer-premium__grid {
      grid-template-columns: 1fr 1fr;
    }
    .obs-footer-premium__brand {
      grid-column: 1 / -1;
    }
    .obs-footer-premium__contact {
      grid-column: 1 / -1;
    }
    .obs-footer-premium__bottom {
      flex-direction: column-reverse;
      gap: 24px;
      text-align: center;
    }
  }

  @media (max-width: 576px) {
    .obs-footer-premium {
      padding: 60px 0 30px;
    }
    .obs-footer-premium__grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }
  }
</style>
